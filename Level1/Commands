	PACKAGE MANAGEMENT WITH APT OR DNF: same command except upgrade
# apt upgrade			#> update the software
# apt dist-upgrade  		#> update the distribution
# apt full-upgrade		#> update everything: the soft and the dist.
# apt search package1		#> search for this package
# apt list package1 		#> lists if the package is in the database
# apt remove package1		#> remove package with unused dependencies by others
# apt autoclean && sudo apt autoremove 	#> remove all unused packages at once 

	ARCHIVES AND INSTALL FROM SOURCE
$ apt install make gcc gzip	#> install the required softwares


$ man -k command1		#> (or $apropos command1) prompts all commands concerning it


	LINUX FILE SYSTEM
$ pwd				#> print working directory
$ cd ~				#> Go to home directory or $cd
.				#> the current directory (ex: cd .)
..				#> the parrent directory (ex: ls ..)
$ mkdir Mydirectory		#> Create a directory
$ rmdir Mydirectory		#> Delete an empty directory
$ rm -r Mydirectory		#> remove directory
$ rm file1			#> remove file1
$ mv file1 file2		#> Rename the file1 to file2  // same option as cp
$ cp file1 file2		#> copy the file1 to another new file2
$ cp -i file1 file2 		#> ask confirmation before overwhite copy
$ cp -n file1 file2 		#> no overwhite copy
$ cp -r direct1 direct 		#> copy directories recursively
$ cp -p file1 file2		#> copy even the time modification 
$ touch file.txt		#> Create empty file
$ more file.txt			#> Prints by page after page
$ less file.txt			#> Prints by page after page with scroll up or down
$ ls -lhaR			#> Long List, human readable, hidden files, and Recursive
$ ls -Str			#> List by size, by time modification, and reversely
$ ls Fil*			#> List all starting with "Fil"
$ ls /etc/????			#> List all in /etc with 4 caracters
$ ls /etc/[abcd]*		#> List all beginning with letters a, b, c or d
$ ls -d /etc/x*			#> List only the directory not its content    
$ /ls				#> List without alias => no color
$ ls /home /etc /otherDir	#> List numerous directories at the same time
$ stat file_or_directory	#> List the details

	LINKS
- Hard links: only in one size, on the same hard drive or partition:
$ ln target_file new_link
- Symbolic links: points to a path, can be on the different hard disks, link breaks after deleting the original file: (1) ->
$ ln -s target_file new_link

	FINDING FILES OR SOMETHING WITH FIND OR LOCATE
$ find /directory -name thingtosearch*	#> search the name
$ find /directory -perm /u=x		#> search things with permission x for 'u' or 'g' or 'o'
$ find /directory -size +2M		#> search with size bigger than 2MB (-2M for <)
$ find /directory -group groupName	#> search things owned by a group
$ find /directory -user userName	#> search things owned by a user
# apt install mlocate
# updatedb				#> locate uses the hard drive so need update before use
$ locate something			#> locates something
$ type command				#> find the type of command like ls
	FINDING FILES OR SOMETHING WITH ECHO (chapter 8)
$ echo /etc/t*				#> display all starting with letter t
$ echo /etc/t??????? 			#> display all with 7 characters after the t
$ echo /etc/*.???			#> all with 3-letter extension
$ echo D*n*s  				#> Begin with D, contains n, and ends with s
$ echo /etc/[gu]*  			#> all starting with g or u (with alphabetical order: g before u)
$ echo /etc/[g-u]*  			#> all between and included g and u 
$ echo /etc/*[gu]*  			#> all containing g or u 
$ echo /etc/*[0-9]*  			#> all containing number 0 to 9
$ echo /etc/[!a-t]*			#> everything not between nor included a or t.
	SEARCHING WITH GREP (GLOBALLY SEARCH FOR REG EXPRESSION & PRINT) (chapter 10)
$ grep thingSearch /dir/file		#> search the word "thingSearch" inside /dir/file
$ cat /dir/file | grep thingSearch	#> same as above
$ grep -r thingSearch /dir/		#> search recursively the word "thingSearch" inside directory's files
$ cat /dir/file | grep -E 'girl|boy'	#> search words girl or boy
$ cat /dir/file | grep -E '^1'		#> (or grep -E '^[1]') #> search words starting with 1
$ cat /dir/file | grep -E '^1[0-2]'	#> search words starting with 1 and followed by numbers between/included 0 or 2
